export const avatarSizes = {
  sm: 32,
  md: 40,
  lg: 56,
} as const;

export const iconSizes = {
  sm: 14,
  md: 18,
  lg: 20,
  xl: 24,
} as const;

export const sizes = { avatar: avatarSizes, icon: iconSizes };
